<plot>
# 设定为直方图
type = heatmap
#type = line
# 用于画直方图的数据文件路径。该文件需要编写程序生成。
# 数据文件为4列：
# chromosome	   start	  end	  data
# Supercontig_1   0       9999    0.489
file = /ifs1/User/pengjx/project/Rice/plot/circos/R900/data/gene_density
# 设置直方图的位置，r1 要比 r0 大。直方图的方向默认为向外。
r1   = 0.74r
r0   = 0.65r
color = blues-9-seq
# 直方图的填充颜色
#fill_color = vlgrey
#color = red
# 默认下直方图轮廓厚度为 1px，若不需要轮廓，则设置其厚度为0，或在 etc/tracks/histogram.conf 中修改。
#thickness = 6p
# 直方图是由 bins （条行框）所构成的。若 bins 在坐标上不相连，最好设置不要将其bins连接到一起。例如：
# hs1 10 20 0.5
# hs1 30 40 0.25
# 上述数据设置值为 yes 和 no 时，图形是不一样的。
#extend_bin = no
# 设置坐标轴的最大值和最小值
#max   = 32
#min   = 1
#
## 设定直方图的背景颜色
#<backgrounds>
#show  = data
#
#<background>
#color = vvlgrey
#</background>
#
#</backgrounds>
</plot>

#<plot>
#type = histogram
#file = snp_density.histogram_for_circos.txt
#r1   = 0.78r
#r0   = 0.66r
#color = red
#thickness = 6p
#extend_bin = no
#max   = 50
#min   = 0
#</plot>
